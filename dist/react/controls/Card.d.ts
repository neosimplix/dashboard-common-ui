import type { ReactNode } from "react";
export type CardProps = {
    className?: string;
    children: ReactNode;
};
export declare function Card({ className, children }: CardProps): import("react").JSX.Element;
