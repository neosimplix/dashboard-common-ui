# ns-table 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 정렬 · 페이징 · 선택이 붙는 표를 `common-ui` 에 추가한다. 표는 데이터가 아니라 **컨트롤과 상태**를 소유하고, 셀 마크업은 소비자가 계속 쓴다.

**Architecture:** 조각 셋이다. `.ns-table` 은 표 표면 클래스, `ns-table` 은 렌더하지 않는 Light DOM 태그(클릭 위임 · `aria-sort` · 전체 선택 3-상태), `ns-pagination` 은 `.ns-button` 을 재사용해 컨트롤을 렌더하는 Light DOM 태그다. **Light DOM 이라 `controls.css` 가 그대로 적용되어 버튼 스타일 중복이 0 이다.**

**Tech Stack:** Lit 3(`ReactiveElement` · `LitElement`), TypeScript 5, Vite 6, `@lit/react`, Node 20+

설계 문서: `docs/superpowers/specs/2026-08-13-ns-table-design.md`
참고 구현: `/Users/neosimplix/coding/dashboard/dashboard-shell/components/ui/Table.tsx` · `Table.module.css`

## Global Constraints

- **테스트 러너를 도입하지 않는다.** `.claude/rules/verification.md` 의 설계 결정이다. vitest·jest·playwright·web-test-runner 를 추가하지 않고 테스트 파일도 만들지 않는다. 회귀 확인 수단은 `npm run check` 와 `index.html` 육안 확인 둘이다.
- **검사를 새로 만들거나 고쳤으면 일부러 깨뜨려 실패를 확인한다.** 그리고 **의도한 이유로 실패했는지**까지 본다.
- **브라우저 확인은 사람이 한다.** 구현자는 화면을 볼 수 없다. **하지 않은 확인을 했다고 보고하지 않는다.**
- **공통 컨트롤 스타일을 재사용해야 하는 컴포넌트는 Light DOM 을 쓴다.** shadow root 를 갖는 이유는 스타일 캡슐화 하나이고, `controls.css` 를 쓰려는 컴포넌트에는 그 캡슐화가 방해다.
- **Light DOM 컴포넌트는 `createRenderRoot()` 를 재정의해 `this` 를 반환한다.** 실패 경로가 둘이다 — Lit 템플릿을 렌더하면 소비자 자식이 **덮어써지고**, shadow root 가 생기면(`ReactiveElement` 의 기본 동작) `<slot>` 이 없어 자식이 **가려진다.** 둘 다 에러 없이 빈 표가 된다.
- **Light DOM 컴포넌트는 `static styles` 를 갖지 않는다.** `createRenderRoot` 를 재정의하면 Lit 의 `adoptStyles` 가 호출되지 않아 조용히 무시된다. 스타일은 전부 `controls.css` 에 있다 — 따라서 이 컴포넌트들에는 `.styles.ts` 파일이 **없다.**
- **컴포넌트 태그는 `ns-` 접두사를 쓴다.** CSS 클래스도 같다. 변형은 `--`, 하위 요소는 `__`.
- **소비자가 쓰는 훅 속성은 `data-ns-` 접두사를 쓴다.** (`data-ns-sort-key` · `data-ns-row-id` · `data-ns-select-all`) Light DOM 이라 문서 이름공간에 들어가고, 충돌하면 엉뚱한 요소를 정렬 헤더로 오인해 **에러 없이 오동작한다.**
- **디자인 토큰 이름에는 접두사를 붙이지 않는다.** 패키지 내부 배선용 커스텀 프로퍼티만 `--ns-` 를 쓴다.
- **`var()` 폴백을 쓰지 않는다.** 색·치수 값은 `tokens.css` 한 곳에만 존재한다.
- **모든 값이 토큰이어야 하는 것은 아니다.** 두 곳 이상에 나타나거나 테마로 바뀔 값만 토큰이다. 한 곳뿐인 구조적 상수는 리터럴로 둔다.
- **제어/비제어는 속성 짝으로 나눈다.** 제어는 프로퍼티 전용, 비제어 초기값은 `default-*` 속성. 하나로 겸용하면 `<ns-pagination page="3">` 이 제어 모드로 들어가 컴포넌트가 스스로 페이지를 넘기지 못한다.
- **제어 중이면 컴포넌트가 그 값을 바꾸지 않는다.** 특히 **제어 모드에서 행 체크박스의 `checked` 를 쓰지 않는다** — React 가 다음 렌더에 덮어쓴다.
- **컴포넌트는 호스트의 속성을 쓰지 않는다.** 예외는 이 스펙이 명시한 둘 — 활성 `<th>` 의 `aria-sort`, 전체 선택 체크박스의 `checked`/`indeterminate`. 그 둘의 **유일한 작성자가 컴포넌트**이므로 소비자와 싸우지 않는다.
- **모든 커스텀 이벤트는 `bubbles: true, composed: true`** 이고 React 래퍼 매핑에 **`EventName<>` 캐스트**가 있어야 한다.
- **`@customElement` 데코레이터를 쓰지 않는다.** `src/internal/register.ts` 를 쓴다.
- **`tsconfig.json` 의 `experimentalDecorators: true` 와 `useDefineForClassFields: false` 를 건드리지 않는다.** `accessor` 를 쓰지 않는다.
- **`index.html` 예시 블록 안에 `<script>` 태그를 넣지 않는다.** HTML 파서는 `type="text/plain"` 과 무관하게 첫 `</script>` 에서 바깥 블록을 닫는다.
- **데모 리스너는 자기가 소유한 엘리먼트에 붙인다. `document.addEventListener` 를 쓰지 않는다.** 배선 코드는 템플릿 복제 루프보다 뒤에 있어야 한다.
- **`index.html` 데모 헬퍼 규약**: `<template class="ex">` → 다음 형제가 `.demo` → 그 다음이 `<pre>`.
- **커밋은 각 Task 끝에서 한 번.** `<type>(<scope>): <한국어 제목>`, 마침표 없음. **`git push` 는 하지 않는다.**

---

## 파일 구조

| 파일 | 책임 |
|---|---|
| `src/controls/controls.css` | `.ns-table` 표면 · `.ns-table-sort` 헤더 버튼과 삼각형 · `ns-table` / `ns-pagination` 요소 선택자 · `.ns-pagination-gap` |
| `src/components/table/ns-table.ts` | `ReactiveElement`. 클릭·change 위임, `aria-sort` 쓰기, 전체 선택 3-상태, `MutationObserver`. **렌더하지 않는다** |
| `src/components/pagination/ns-pagination.ts` | `LitElement` + light DOM 렌더. 번호 윈도우 계산, 컨트롤 렌더 |
| `src/types.ts` | `NsSortDirection` · `NsSortDetail` · `NsSelectChangeDetail` · `NsPageChangeDetail` + `HTMLElementEventMap` 확장 |
| `src/index.ts` | 등록 부수효과 import + 클래스·타입 재export |
| `src/react/elements.ts` | `NsTable` · `NsPagination` 래퍼. 이벤트 매핑의 단일 출처 |
| `src/react/index.ts` | 재export 허브 |
| `scripts/check-controls.mjs` | 요소 선택자까지 정방향 대조 |
| `index.html` | 섹션 셋 + 데모 배선 |
| `docs/consumer-example.tsx` | 세 이벤트의 `e.detail` 을 직접 읽는다 |

**두 컴포넌트에 `.styles.ts` 가 없다.** Light DOM 이라 `static styles` 가 무시되기 때문이고, 이는 "로직과 스타일을 파일 두 개로 나눈다" 규칙의 명시적 예외다. Task 5 에서 규칙 문서에 적는다.

**React 편의 컴포넌트를 만들지 않는다.** 소비자는 `NsTable` 래퍼와 `<table className="ns-table">` 을 직접 쓴다. `Table` 같은 shim 을 두면 `ns-dialog` 에서 물린 문제가 재발한다 — shim 이 이벤트를 어댑팅하면 `EventName<>` 캐스트 검사가 그 안으로 옮겨간다. 여기서는 프롭 이름 충돌이 없어 shim 이 필요 없다.

## 범위

정렬 · 페이징 · 선택과 그 스타일까지다. **`dashboard-shell` 표 9곳 이관은 이 계획에 없다** — 다른 저장소이고 별도 스펙이다. 이관 모양은 설계 문서 §4.2 에 있다.

---

### Task 1: `.ns-table` 스타일과 `check-controls.mjs` 확장

스타일과 검사를 함께 넣는다. `controls.css` 에 **요소 선택자**가 처음 들어가는데 지금 검사는 점이 붙은 클래스만 잡으므로, 검사를 먼저 넓히지 않으면 새 스타일이 문서 대조 밖에 놓인다.

**Files:**
- Modify: `src/controls/controls.css`
- Modify: `scripts/check-controls.mjs`
- Modify: `index.html` (nav 항목 · `.ns-table` 섹션)

**Interfaces:**
- Produces:
  - 클래스 `.ns-table` · `.ns-table-sort`
  - 요소 선택자 `ns-table`
  - `scripts/check-controls.mjs` 가 `^\s*(ns-[a-z-]+)\s*[,{]` 형태의 요소 선택자도 정방향 대조한다

- [ ] **Step 1: `check-controls.mjs` 에 요소 선택자 추출을 추가한다**

기존 `defined` 계산 **뒤**에 넣고, `missing` 계산에서 함께 쓴다. 파일 상단 주석에도 한 단락을 더한다.

```js
/*
  요소 선택자도 잡는다.

  ns-table · ns-pagination 은 Light DOM 컴포넌트라 controls.css 가 요소
  선택자로 직접 스타일한다(`ns-table { overflow-x: auto }`). 점이 붙은 클래스만
  찾으면 그 규칙들이 문서 대조 밖에 놓인다.

  정방향만 본다 — CSS 에 있으면 문서에도 있어야 한다. 역방향은 넣지 않는다.
  태그 이름은 index.html 전체에 정당하게 등장하므로(데모 마크업, 프로퍼티 표,
  예시 블록) 역방향으로 보면 거짓 양성만 쏟아진다.
*/
const ELEMENT = /^[ \t]*(ns-[a-z-]+)[ \t]*[,{]/gm;

for (const m of css.matchAll(ELEMENT)) defined.add(m[1]);
```

`defined` 가 `const defined = new Set(...)` 로 선언돼 있어야 `add` 가 가능하다. 이미 `new Set(...)` 이므로 그대로 쓴다.

문서 쪽 추출(`documented`)은 손대지 않는다. 요소 선택자는 `index.html` 에 `<ns-table>` 마크업이나 `<code>ns-table</code>` 로 등장하므로, `documented` 에 태그 이름을 넣어 줄 필요가 있다. **`class="..."` 와 점 붙은 형태만 보는 현재 추출로는 `ns-table` 이 documented 에 들어가지 않는다.** 그래서 한 줄을 더한다.

```js
/*
  요소 선택자의 문서화 여부는 태그가 등장하는지로 본다 — <ns-table> 마크업이든
  <code>ns-table</code> 언급이든 상관없다. defined 에 있는 이름만 확인하므로
  (documented 에 넣는 것이 아니라) 역방향 거짓 양성이 생기지 않는다.
*/
const TAG = (name) => new RegExp(`<${name}[\\s>]|<code>${name}</code>`);
```

그리고 `missing` 계산을 이렇게 바꾼다.

```js
const missing = [...defined]
  .filter((n) => !documented.has(n) && !TAG(n).test(html))
  .sort();
```

- [ ] **Step 2: `controls.css` 에 표 스타일을 추가한다**

`@layer ns-controls` 블록 **안**, `.ns-card` 규칙 **앞**에 넣는다.

```css
  /*
    ns-table 이 스크롤 컨테이너를 겸한다. 참고 구현은 <div class="scroll"> 로
    감쌌지만, 래퍼용 클래스를 따로 두면 "정렬은 필요 없고 스타일만" 인 경우와
    두 가지 쓰는 법이 생긴다.

    요소 선택자는 정의되지 않은 커스텀 엘리먼트에도 적용되므로 JS 없이도
    스타일이 맞다. 스타일은 즉시, 동작은 JS 로드 시 붙는다.
  */
  ns-table {
    display: block;
    overflow-x: auto;
  }

  .ns-table {
    width: 100%;
    /*
      overflow-x: auto 는 표가 컨테이너를 넘을 수 있을 때만 발동한다.
      width: 100% 만 있으면 넘칠 수가 없어 브라우저가 칸을 쥐어짜 맞추고,
      액션 칸의 버튼이 폭을 잃어 글자가 줄바꿈된다. max-content 는 "내용이
      필요로 하는 폭" 이다.
    */
    min-width: max-content;
    border-collapse: collapse;
    font-size: var(--font-size-sm);
    line-height: var(--line-height-sm);
  }

  .ns-table th {
    padding: var(--space-2) var(--space-3);
    text-align: left;
    font-size: var(--font-size-xs);
    line-height: var(--line-height-xs);
    font-weight: var(--weight-semibold);
    color: var(--color-fg-muted);
    border-bottom: 1px solid var(--color-line);
    white-space: nowrap;
  }

  .ns-table td {
    padding: var(--space-3);
    color: var(--color-fg-body);
    border-bottom: 1px solid var(--color-line);
    vertical-align: middle;
  }

  .ns-table tbody tr:last-child td {
    border-bottom: 0;
  }

  .ns-table tbody tr:hover {
    background: var(--color-surface-hover);
  }

  /*
    정렬 헤더는 <th> 안의 <button> 이다. <th> 자체를 클릭 가능하게만 만들면
    키보드로 도달할 수 없고 화면낭독기가 상호작용 요소로 안내하지 않는다.
    tabindex/role=button 을 <th> 에 얹는 것은 표 의미를 깨뜨린다.

    th 의 글꼴·색을 상속하는 텍스트 버튼이다 — .ns-button 을 쓰지 않는 이유는
    크기·정렬·삼각형 자리가 다르기 때문이다.
  */
  .ns-table-sort {
    display: inline-flex;
    align-items: center;
    gap: var(--space-1-5);
    padding: 0;
    border: 0;
    background: none;
    font: inherit;
    color: inherit;
    text-align: left;
    cursor: pointer;
  }

  .ns-table-sort:hover {
    color: var(--color-fg);
  }

  /*
    삼각형을 border 로 그린다. content 로 화살표 문자를 넣지 않는다 — 생성된
    텍스트가 일부 브라우저에서 화면낭독기에 읽혀 aria-sort 와 중복 안내가 된다.
    border 로 그린 삼각형은 텍스트 노드가 없어 안내되지 않고, 의미는 aria-sort
    하나가 담는다.

    자리는 정렬 여부와 무관하게 늘 잡아 둔다. 안 그러면 정렬할 때마다 헤더
    폭이 흔들린다.
  */
  .ns-table-sort::after {
    content: "";
    width: 0;
    height: 0;
    border-inline: .25rem solid transparent;
    border-block-end: .3rem solid currentColor;
    opacity: 0;
  }

  .ns-table th[aria-sort="ascending"] .ns-table-sort::after {
    opacity: 1;
  }

  .ns-table th[aria-sort="descending"] .ns-table-sort::after {
    opacity: 1;
    rotate: 180deg;
  }

```

- [ ] **Step 2b: `.ns-table-sort` 를 기존 공용 `:focus-visible` 블록에 합류시킨다**

`controls.css` 에 이미 공용 포커스 규칙이 있다.

```css
  .ns-button:focus-visible,
  .ns-input:focus-visible,
  .ns-textarea:focus-visible,
  .ns-select:focus-visible {
    outline: 2px solid var(--color-accent);
    outline-offset: 2px;
  }
```

**`.ns-table-sort` 를 위한 별도 규칙을 만들지 않는다.** 같은 선언을 두 곳에 두면 어긋나고, 어긋나도 아무도 모른다 — `library-invariants.md` 의 "값이 두 곳에 존재하면" 항목이 그것이다. 이 저장소는 같은 상황(`.ns-select` 가 공통 블록을 복사)을 이미 한 번 합쳐서 정리했다.

셀렉터 목록에 한 줄을 더한다.

```css
  .ns-button:focus-visible,
  .ns-input:focus-visible,
  .ns-textarea:focus-visible,
  .ns-select:focus-visible,
  .ns-table-sort:focus-visible {
```

그 블록 위 주석의 첫 문장("텍스트 컨트롤은 위에서 outline: none 으로 …")은 그대로 둔다 — `.ns-table-sort` 는 `outline: none` 을 갖지 않지만, 애초에 포커스 규칙이 없었다는 점에서 버튼과 같은 처지다. 주석 마지막에 한 문장을 더한다.

```
    .ns-table-sort 도 여기 있다 — th 안의 텍스트 버튼이라 배경이 없고,
    링이 없으면 정렬 헤더에 키보드로 도달했는지 알 수 없다.
```

- [ ] **Step 3: `index.html` 에 네비게이션 항목과 섹션을 추가한다**

"클래스" 그룹의 `.ns-card` 항목 **앞**에 넣는다.

```html
      <ns-nav-item href="#ns-table" label=".ns-table" badge="TB"></ns-nav-item>
```

`<h2 id="ns-card">` 바로 **앞**에 섹션을 넣는다.

```html
  <h2 id="ns-table">.ns-table</h2>
  <p>
    표 표면. 정렬·페이징·선택 <strong>동작</strong>은 <code>ns-table</code> 태그가 맡고
    (아래 별도 절), 이 클래스는 테두리·패딩·hover 만 갖는다.
  </p>
  <p>
    <strong>스크롤 컨테이너는 <code>ns-table</code> 요소다.</strong> 좁은 화면에서 표가
    부모를 밀어내지 않게 하고, <code>min-width: max-content</code> 와 짝을 이뤄
    액션 칸의 버튼이 쥐어짜여 줄바꿈되는 것을 막는다. 요소 선택자로 스타일하므로
    <strong>JS 없이도 동작한다</strong> — 정의되지 않은 커스텀 엘리먼트에도 적용된다.
  </p>

  <h3>데모</h3>
  <template class="ex">
    <ns-table>
      <table class="ns-table">
        <thead>
          <tr><th>이름</th><th>부서</th><th>가입일</th></tr>
        </thead>
        <tbody>
          <tr><td>홍길동</td><td>마케팅팀</td><td>2026-07-15</td></tr>
          <tr><td>김대기</td><td>플랫폼개발팀</td><td>2026-08-05</td></tr>
        </tbody>
      </table>
    </ns-table>
  </template>
  <div class="demo block" id="table-demo" style="padding: var(--space-4)"></div>
  <pre></pre>

  <h3>좁은 컨테이너</h3>
  <p>
    칸이 쥐어짜이는 대신 가로로 스크롤되는지 보는 자리다. 이 조합이 실제로 깨졌던
    화면(관리자 프로젝트 표)의 축소판이다.
  </p>
  <template class="ex">
    <ns-table>
      <table class="ns-table">
        <thead>
          <tr><th>이름</th><th>주소</th><th>URL</th><th>부서</th><th>담당자</th><th>상태</th><th></th></tr>
        </thead>
        <tbody>
          <tr>
            <td>글로벌 인플루언서 마케팅</td>
            <td>global-influencer-marketing</td>
            <td>https://dash.example.com/global</td>
            <td>마케팅팀, 플랫폼개발팀</td>
            <td>담당자 둘</td>
            <td>운영 중</td>
            <td>
              <span style="display:inline-flex;gap:var(--space-2)">
                <button class="ns-button ns-button--ghost ns-button--sm" type="button">문의</button>
                <button class="ns-button ns-button--outline ns-button--sm" type="button">수정</button>
                <button class="ns-button ns-button--ghost ns-button--sm" type="button">삭제</button>
              </span>
            </td>
          </tr>
        </tbody>
      </table>
    </ns-table>
  </template>
  <div class="demo block" id="table-narrow-demo" style="max-width:26rem;padding:var(--space-4)"></div>
  <pre></pre>

  <h3>클래스</h3>
  <table class="ns-table">
    <tr><th>이름</th><th>붙이는 요소</th><th>설명</th></tr>
    <tr><td><code>ns-table</code></td><td>(요소 선택자)</td><td>스크롤 컨테이너. 클래스가 아니라 태그 자체를 스타일한다</td></tr>
    <tr><td><code>.ns-table</code></td><td><code>table</code></td><td>표면. <code>th</code>·<code>td</code>·hover 는 자손 선택자로 잡혀 클래스가 필요 없다</td></tr>
    <tr><td><code>.ns-table-sort</code></td><td><code>th</code> 안의 <code>button</code></td><td>정렬 헤더. 삼각형 자리를 늘 잡아 두어 정렬 시 헤더 폭이 흔들리지 않는다</td></tr>
  </table>

  <h3>HTML</h3>
  <script type="text/plain">
    <ns-table>
      <table class="ns-table">
        <thead><tr><th>이름</th></tr></thead>
        <tbody><tr><td>홍길동</td></tr></tbody>
      </table>
    </ns-table>
  </script>

  <h3>React</h3>
  <script type="text/plain">
    import { NsTable } from "@neosimplix/common-ui/react";

    /* 셀 내용에 제약이 없다 — 표는 데이터를 모른다 */
    <NsTable>
      <table className="ns-table">
        <thead><tr><th>이름</th><th /></tr></thead>
        <tbody>
          {records.map((r) => (
            <tr key={r.email}>
              <td>{r.name}</td>
              <td><Button size="sm" onClick={() => onEdit(r)}>수정</Button></td>
            </tr>
          ))}
        </tbody>
      </table>
    </NsTable>
  </script>
```

- [ ] **Step 4: `npm run check` 를 돌린다**

Run: `npm run check`
Expected: 네 단계 통과. ④의 클래스 목록에 `ns-table` · `ns-table-sort` 가 포함된다 (`ns-table` 은 요소 선택자로도, 클래스로도 정의돼 한 번만 나온다).

- [ ] **Step 5: 요소 선택자 추출을 깨뜨려 실패를 확인한다**

`controls.css` 의 `@layer` 블록 안에 임시로 넣는다.

```css
  ns-fake { display: block; }
```

Run: `npm run check`
Expected: FAIL — `index.html 에 문서화되지 않은 클래스: ns-fake`

**의도한 이유로 실패했는지 본다.** 타입 에러나 이벤트 매핑 에러가 먼저 나면 이 속성은 검증되지 않은 것이다. 확인 후 그 두 줄을 지운다.

- [ ] **Step 6: 태그 문서화 판정이 실제로 동작하는지 확인한다**

`index.html` 의 `.ns-table` 섹션에서 `<ns-table>` 을 쓰는 모든 곳과 `<code>ns-table</code>` 을 임시로 지운다 (데모 두 개의 `<ns-table>` 래퍼, 클래스 표의 `<code>ns-table</code>`).

Run: `npm run check`
Expected: FAIL — `index.html 에 문서화되지 않은 클래스: ns-table`

이 확인이 필요한 이유는, `TAG()` 정규식이 잘못 쓰이면 **아무것도 매치하지 않아 늘 실패하거나, 너무 넓어 늘 통과**하기 때문이다. 확인 후 되돌린다.

- [ ] **Step 7: 빌드하고 확인한다**

Run: `npm run build`

```sh
grep -c 'ns-table' dist/controls.css        # 1 이상
grep -c 'max-content' dist/controls.css     # 1 — 액션 칸 보호 규칙이 살아있는지
grep -c 'aria-sort' dist/controls.css       # 2 — 삼각형 규칙 둘
```

- [ ] **Step 8: 구조 검사를 돌린다**

```sh
grep -c '<script>' index.html                                        # 1
grep -n '</script>' index.html | grep -v -E ':\s*</script>\s*$' | grep -v '<script src='   # 출력 없음
grep -n 'document.addEventListener' index.html                        # 출력 없음
```

- [ ] **Step 9: 사람이 확인할 것을 보고서에 적는다**

- `.ns-table` 데모의 테두리·패딩·hover 가 보이는지
- **좁은 컨테이너 데모에서 표가 가로로 스크롤되고 액션 버튼 세 개가 줄바꿈되지 않는지** — 이 규칙이 존재하는 이유다
- 클래스 표 자체가 `.ns-table` 로 스타일돼 있으니 문서가 자기 컴포넌트로 만들어졌는지
- **`dist/` 를 지우고 `tokens.css`·`controls.css` 만 링크한 상태에서** `<ns-table>` 이 스크롤 컨테이너로 동작하는지 (JS 없이)

- [ ] **Step 10: 커밋**

```bash
git add -A
git commit -m "feat(table): .ns-table 스타일과 요소 선택자 대조 추가"
```

---

### Task 2: `ns-table` — 정렬

첫 Light DOM 컴포넌트다. 이 라이브러리에 세 번째 컴포넌트 유형이 생기고, `createRenderRoot` 재정의가 두 실패 경로를 모두 막는지가 이 Task 의 핵심이다.

**Files:**
- Create: `src/components/table/ns-table.ts`
- Modify: `src/types.ts`
- Modify: `src/index.ts`
- Modify: `src/react/elements.ts`
- Modify: `src/react/index.ts`
- Modify: `index.html` (nav 항목 · 섹션 · 헬퍼 배선)
- Modify: `docs/consumer-example.tsx`

**Interfaces:**
- Consumes: `.ns-table` · `.ns-table-sort` (Task 1), `register()` · `warnIfTokensMissing()` (기존)
- Produces:
  - `src/types.ts` → `export type NsSortDirection = "ascending" | "descending" | "none"`, `export interface NsSortDetail { key: string; direction: NsSortDirection }`
  - `src/components/table/ns-table.ts` → `export class NsTable extends ReactiveElement` with `sortKey?: string`, `sortDirection?: NsSortDirection`, `defaultSortKey: string`, `defaultSortDirection: NsSortDirection`
  - `src/react/elements.ts` → `export const NsTable` (이벤트 `onNsSort`)
  - Task 3 이 같은 클래스에 선택 기능을 더한다 — `connectedCallback` 의 리스너 등록과 `updated()` 를 그 Task 가 확장한다

- [ ] **Step 1: `src/types.ts` 에 타입을 추가한다**

기존 인터페이스들 **뒤**, `declare global` 블록 **앞**에 넣는다.

```ts
/** ns-table 의 정렬 방향. */
export type NsSortDirection = "ascending" | "descending" | "none";

/**
 * ns-table 의 정렬 헤더 클릭이 올리는 이벤트.
 *
 * `direction` 이 `"none"` 이면 `key` 는 빈 문자열이다 — 정렬이 해제된 상태다.
 * **데이터를 다시 정렬하는 것은 소비자다.** 컴포넌트는 어느 칼럼이 어느 방향인지와
 * 그 시각 표시만 갖는다.
 */
export interface NsSortDetail {
  key: string;
  direction: NsSortDirection;
}
```

`declare global` 블록에 한 줄 추가한다.

```ts
    "ns-sort": CustomEvent<NsSortDetail>;
```

- [ ] **Step 2: `src/components/table/ns-table.ts` 를 만든다**

```ts
import { ReactiveElement } from "lit";
import { property } from "lit/decorators.js";

import { register } from "../../internal/register.js";
import { warnIfTokensMissing } from "../../internal/warn-missing-tokens.js";
import type { NsSortDetail, NsSortDirection } from "../../types.js";

/*
  none → ascending → descending → none.

  2-상태(오름·내림만)로 하면 서버의 기본 정렬로 돌아갈 방법이 없다. 이 앱에서
  그 기본 순서가 의도를 담는다 — "오래된 신청이 위" 같은 것이다. 이름으로 한 번
  정렬하면 새로고침 말고는 되돌릴 수 없게 된다.
*/
function nextDirection(current: NsSortDirection): NsSortDirection {
  if (current === "none") return "ascending";
  if (current === "ascending") return "descending";
  return "none";
}

/**
 * 소비자가 쓴 `<table>` 을 감싸는 Light DOM 컴포넌트.
 *
 * **셀을 렌더하지 않고 데이터를 모른다.** 정렬 상태와 그 시각 표시만 갖고,
 * 데이터를 다시 정렬하는 것은 소비자다. 설계 근거는
 * docs/superpowers/specs/2026-08-13-ns-table-design.md §2 에 있다 — 실사용 셀에
 * 조건부 버튼과 참조 조회가 들어 있어 데이터 주입형 API 로 표현되지 않는다.
 */
export class NsTable extends ReactiveElement {
  /*
    Light DOM 이다. 실패 경로가 둘이고 이 재정의가 둘 다 막는다.

    1. LitElement 처럼 템플릿을 렌더하면 소비자가 쓴 <table> 이 덮어써진다.
       → ReactiveElement 를 상속해 렌더 파이프라인 자체를 갖지 않는다.
    2. ReactiveElement 의 기본 createRenderRoot 는 shadow root 를 만든다.
       shadow root 가 있으면 <slot> 이 없는 한 light DOM 자식이 렌더되지 않는다.
       → this 를 반환해 shadow root 를 만들지 않는다.

    둘 다 에러 없이 빈 표가 된다. 그래서 이 재정의를 지우면 조용히 깨진다.

    부수 효과로 static styles 가 무시된다(adoptStyles 가 호출되지 않는다).
    스타일은 전부 controls.css 에 있고 이 컴포넌트에 .styles.ts 파일이 없다.
  */
  protected override createRenderRoot(): HTMLElement {
    return this;
  }

  /**
   * 제어 모드의 정렬 칼럼. `undefined` 면 비제어다.
   *
   * 속성이 아니라 프로퍼티 전용인 이유는 ns-dialog 의 `open` 과 같다 —
   * `<ns-table sort-key="name">` 이라고 쓰면 제어 모드로 들어가 컴포넌트가
   * 스스로 방향을 바꾸지 못한다. 순수 HTML 은 `default-sort-key` 를 쓴다.
   */
  @property({ attribute: false }) sortKey?: string;

  /** 제어 모드의 정렬 방향. `sortKey` 와 짝이다. */
  @property({ attribute: false }) sortDirection?: NsSortDirection;

  /** 비제어 초기 정렬 칼럼. */
  @property({ type: String, attribute: "default-sort-key" }) defaultSortKey = "";

  /** 비제어 초기 정렬 방향. */
  @property({ attribute: "default-sort-direction" })
  defaultSortDirection: NsSortDirection = "none";

  /** 비제어일 때의 진실. */
  #innerKey = "";
  #innerDirection: NsSortDirection = "none";

  get #controlled(): boolean {
    return this.sortKey !== undefined;
  }

  get #key(): string {
    return this.sortKey ?? this.#innerKey;
  }

  get #direction(): NsSortDirection {
    return this.sortDirection ?? this.#innerDirection;
  }

  override connectedCallback(): void {
    super.connectedCallback();
    warnIfTokensMissing();
    // 위임이라 소비자가 행을 다시 그려도 리스너를 다시 붙일 필요가 없다.
    this.addEventListener("click", this.#onClick);
  }

  override disconnectedCallback(): void {
    this.removeEventListener("click", this.#onClick);
    super.disconnectedCallback();
  }

  /*
    비제어 초기값을 seed 한다. 덮어쓰지 않는다 — Lit 은 첫 업데이트를
    마이크로태스크로 미루므로, 생성과 같은 태스크에서 프로퍼티를 만진 코드가
    여기보다 먼저 실행된다. 무조건 대입하면 그것이 경고 없이 사라진다.
  */
  protected override firstUpdated(): void {
    if (this.defaultSortKey !== "") this.#innerKey = this.defaultSortKey;
    if (this.defaultSortDirection !== "none") this.#innerDirection = this.defaultSortDirection;
  }

  protected override updated(): void {
    this.#syncAriaSort();
  }

  /*
    활성 <th> 에 aria-sort 를 쓴다. 컴포넌트가 유일한 작성자다 — 소비자는 이
    속성을 쓰지 않으므로 React 와 싸우지 않는다. 삼각형은 controls.css 가
    이 속성을 받아 그린다.
  */
  #syncAriaSort(): void {
    const key = this.#key;
    const direction = this.#direction;

    for (const th of this.querySelectorAll<HTMLElement>("th[data-ns-sort-key]")) {
      th.setAttribute("aria-sort", th.dataset.nsSortKey === key ? direction : "none");
    }
  }

  /*
    <th data-ns-sort-key> 안의 클릭을 받는다. 안쪽 <button> 뿐 아니라 <th> 의
    여백을 눌러도 동작한다 — 마우스 타깃이 넓어지고, 키보드 도달은 <button> 이
    담당한다. 소비자가 훅을 붙인 <th> 만 대상이다.
  */
  #onClick = (e: Event): void => {
    const target = e.target as Element | null;
    const th = target?.closest<HTMLElement>("th[data-ns-sort-key]");
    if (!th) return;

    const key = th.dataset.nsSortKey ?? "";
    // 다른 칼럼을 누르면 오름차순부터 시작한다. 같은 칼럼이면 다음 상태로 돈다.
    const direction = key === this.#key ? nextDirection(this.#direction) : "ascending";
    // direction 이 none 이면 정렬이 해제된 것이므로 key 를 비운다.
    const nextKey = direction === "none" ? "" : key;

    if (!this.#controlled) {
      this.#innerKey = nextKey;
      this.#innerDirection = direction;
      this.requestUpdate();
    }

    const detail: NsSortDetail = { key: nextKey, direction };
    this.dispatchEvent(
      new CustomEvent("ns-sort", { detail, bubbles: true, composed: true }),
    );
  };
}

register("ns-table", NsTable);

declare global {
  interface HTMLElementTagNameMap {
    "ns-table": NsTable;
  }
}
```

- [ ] **Step 3: `src/index.ts` 에 등록과 재export 를 추가한다**

import 는 알파벳 순을 유지한다 (`skeleton` 다음).

```ts
import "./components/table/ns-table.js";
```

```ts
export { NsTable } from "./components/table/ns-table.js";
```

타입 재export 줄을 넓힌다.

```ts
export type {
  NsToggleDetail,
  NsNavigateDetail,
  NsDialogCloseDetail,
  NsDialogCloseReason,
  NsSortDetail,
  NsSortDirection,
} from "./types.js";
```

- [ ] **Step 4: `src/react/elements.ts` 에 래퍼를 추가한다**

```ts
import { NsTable as NsTableElement } from "../components/table/ns-table.js";
```

타입 import 를 넓힌다.

```ts
import type {
  NsToggleDetail,
  NsNavigateDetail,
  NsDialogCloseDetail,
  NsSortDetail,
} from "../types.js";
```

```ts
/*
  shim 이 필요 없다. sortKey·sortDirection 어느 것도 HTML 전역 속성과 충돌하지
  않으므로 평범한 래퍼를 그대로 공개한다. 그래서 EventName<> 검사가 고전적인
  경로로 동작한다 — docs/consumer-example.tsx 가 e.detail 을 직접 읽는다.
*/
export const NsTable = createComponent({
  react: React,
  tagName: "ns-table",
  elementClass: NsTableElement,
  events: {
    // EventName<> 브랜딩이 없으면 핸들러가 (e: Event) => void 로 타입된다.
    onNsSort: "ns-sort" as EventName<CustomEvent<NsSortDetail>>,
  },
});
```

- [ ] **Step 5: `src/react/index.ts` 에 재export 를 추가한다**

태그 export 줄에 `NsTable` 을 알파벳 위치에 넣는다.

```ts
export { NsHeader, NsIcon, NsNavGroup, NsNavItem, NsSidebar, NsSkeleton, NsTable } from "./elements.js";
```

타입 재export 줄을 넓힌다.

```ts
export type {
  NsToggleDetail,
  NsNavigateDetail,
  NsDialogCloseDetail,
  NsDialogCloseReason,
  NsSortDetail,
  NsSortDirection,
} from "../types.js";
```

- [ ] **Step 6: `index.html` 에 항목과 섹션을 추가한다**

"컴포넌트" 그룹의 `ns-dialog` 항목 **다음**에 넣는다.

```html
      <ns-nav-item href="#ns-table-tag" label="ns-table" badge="TT"></ns-nav-item>
```

`<h2 id="ns-header">` 바로 **앞**에 섹션을 넣는다. id 가 `ns-table-tag` 인 이유는 `.ns-table` 클래스 절이 이미 `ns-table` 을 쓰고 있기 때문이다.

```html
  <h2 id="ns-table-tag">ns-table</h2>
  <p>
    소비자가 쓴 <code>table</code> 을 감싸는 Light DOM 컴포넌트.
    <strong>셀을 렌더하지 않고 데이터를 모른다.</strong> 정렬 상태와 그 시각 표시만 갖고,
    데이터를 다시 정렬하는 것은 소비자다.
  </p>
  <p>
    그렇게 나눈 이유는 실사용 셀이 데이터 주입형 API 로 표현되지 않기 때문이다 —
    조건부 버튼 여섯, 참조 조회로 만든 이름, 조건부 칼럼이 들어 있다.
    <strong>Lit 엘리먼트는 소비자의 React 렌더 함수를 호출해 자기 안에 React 요소를
    만들 수 없다.</strong>
  </p>

  <h3>정렬 헤더는 <code>button</code> 을 품는다</h3>
  <p>
    <code>th</code> 자체를 클릭 가능하게만 만들면 <strong>키보드로 도달할 수 없고</strong>
    화면낭독기가 상호작용 요소로 안내하지 않는다. <code>tabindex</code>/<code>role="button"</code> 을
    <code>th</code> 에 얹는 것은 표 의미를 깨뜨린다. 컴포넌트가 렌더를 하지 않으므로
    이것을 대신 만들어 줄 수 없다 — 셀을 소비자에게 남긴 대가다.
  </p>
  <p>
    <code>data-ns-sort-key</code> 가 없는 <code>th</code> 는 정렬되지 않는다. 칼럼별 스위치가 그것뿐이다.
  </p>

  <h3>데모</h3>
  <template class="ex">
    <ns-table>
      <table class="ns-table">
        <thead>
          <tr>
            <th data-ns-sort-key="name"><button class="ns-table-sort" type="button">이름</button></th>
            <th>부서</th>
            <th data-ns-sort-key="joined"><button class="ns-table-sort" type="button">가입일</button></th>
          </tr>
        </thead>
        <tbody>
          <tr><td>홍길동</td><td>마케팅팀</td><td>2026-07-15</td></tr>
          <tr><td>김대기</td><td>플랫폼개발팀</td><td>2026-08-05</td></tr>
          <tr><td>박서준</td><td>데이터분석팀</td><td>2026-06-02</td></tr>
        </tbody>
      </table>
    </ns-table>
  </template>
  <div class="demo block" id="sort-demo" style="padding: var(--space-4)"></div>
  <pre></pre>
  <p id="sort-log" style="font-family: monospace">헤더를 누르면 여기에 이벤트가 찍힌다.</p>
  <p>
    "부서" 는 <code>data-ns-sort-key</code> 가 없어 정렬되지 않는다.
    같은 헤더를 세 번 누르면 <strong>정렬이 해제되어 원래 순서로 돌아온다</strong>.
  </p>

  <h3>프로퍼티</h3>
  <table class="ns-table">
    <tr><th>프로퍼티</th><th>속성</th><th>타입</th><th>기본값</th><th>설명</th></tr>
    <tr><td><code>sortKey</code></td><td>없음</td><td>string | undefined</td><td><code>undefined</code></td><td>제어 모드. <strong>속성이 아니라 프로퍼티다</strong></td></tr>
    <tr><td><code>sortDirection</code></td><td>없음</td><td>"ascending" | "descending" | "none" | undefined</td><td><code>undefined</code></td><td><code>sortKey</code> 와 짝</td></tr>
    <tr><td><code>defaultSortKey</code></td><td><code>default-sort-key</code></td><td>string</td><td><code>""</code></td><td>비제어 초기값</td></tr>
    <tr><td><code>defaultSortDirection</code></td><td><code>default-sort-direction</code></td><td>같음</td><td><code>"none"</code></td><td>비제어 초기값</td></tr>
  </table>

  <h3>소비자가 쓰는 훅</h3>
  <table class="ns-table">
    <tr><th>속성</th><th>붙이는 요소</th><th>설명</th></tr>
    <tr><td><code>data-ns-sort-key</code></td><td><code>th</code></td><td>이 칼럼을 정렬 대상으로 만든다. 값이 <code>ns-sort</code> 의 <code>key</code> 로 실린다</td></tr>
  </table>

  <h3>이벤트</h3>
  <table class="ns-table">
    <tr><th>이름</th><th>detail</th><th>발생 시점</th></tr>
    <tr><td><code>ns-sort</code></td><td><code>{ key: string, direction: "ascending" | "descending" | "none" }</code></td><td>정렬 헤더 클릭. <code>direction</code> 이 <code>"none"</code> 이면 <code>key</code> 는 빈 문자열이다</td></tr>
  </table>

  <h3>방향은 3-상태다</h3>
  <p>
    <code>none → ascending → descending → none</code>.
    2-상태로 하면 <strong>서버의 기본 정렬로 돌아갈 방법이 없다.</strong> 그 기본 순서가
    의도를 담는 경우가 있다 — "오래된 신청이 위" 같은 것이다.
  </p>

  <h3>접근성</h3>
  <p>
    <code>aria-sort</code> 의 <strong>유일한 작성자가 컴포넌트다.</strong> 소비자는 쓰지 않는다.
    삼각형은 <code>content</code> 문자가 아니라 <code>border</code> 로 그린다 — 생성된 텍스트는
    일부 브라우저에서 화면낭독기에 읽혀 <code>aria-sort</code> 와 중복 안내가 된다.
  </p>

  <h3>HTML — 마크업</h3>
  <script type="text/plain">
    <ns-table>
      <table class="ns-table">
        <thead><tr>
          <th data-ns-sort-key="name"><button class="ns-table-sort" type="button">이름</button></th>
          <th>이메일</th>
        </tr></thead>
        <tbody><!-- 소비자가 쓴 그대로 --></tbody>
      </table>
    </ns-table>
  </script>

  <h3>HTML — 배선</h3>
  <script type="text/plain">
    document.querySelector("ns-table").addEventListener("ns-sort", (e) => {
      /* 데이터를 다시 정렬하는 것은 소비자다 */
      redraw(sortRows(rows, e.detail.key, e.detail.direction));
    });
  </script>

  <h3>React</h3>
  <script type="text/plain">
    import { NsTable } from "@neosimplix/common-ui/react";

    const [sort, setSort] = useState({ key: "", direction: "none" as const });

    <NsTable
      sortKey={sort.key}
      sortDirection={sort.direction}
      onNsSort={(e) => setSort(e.detail)}
    >
      <table className="ns-table">
        <thead><tr>
          <th data-ns-sort-key="name"><button className="ns-table-sort" type="button">이름</button></th>
        </tr></thead>
        <tbody>{sortRows(records, sort).map(/* … */)}</tbody>
      </table>
    </NsTable>
  </script>
```

- [ ] **Step 7: `index.html` 헬퍼 스크립트에 데모 배선을 추가한다**

기존 `<script>` 안, 대화상자 배선 블록 **다음**(스크립트 닫는 `</script>` 바로 앞)에 넣는다. 템플릿 복제 루프보다 뒤여야 복제된 노드를 찾을 수 있다.

```js
  const sortDemo = document.getElementById("sort-demo");
  const sortLog = document.getElementById("sort-log");
  if (sortDemo && sortLog) {
    const sortTable = sortDemo.querySelector("ns-table");
    const sortBody = sortDemo.querySelector("tbody");
    // 원래 순서를 보존한다 — 3-상태의 "none" 이 이 순서로 돌아오는 것을 보여준다.
    const sortRows = [...sortBody.querySelectorAll("tr")].map((tr) => ({
      name: tr.cells[0].textContent,
      dept: tr.cells[1].textContent,
      joined: tr.cells[2].textContent,
    }));

    const drawSort = (key, direction) => {
      const list = [...sortRows];
      if (direction !== "none") {
        list.sort((a, b) => a[key].localeCompare(b[key], "ko"));
        if (direction === "descending") list.reverse();
      }
      sortBody.replaceChildren(
        ...list.map((r) => {
          const tr = document.createElement("tr");
          for (const value of [r.name, r.dept, r.joined]) {
            const td = document.createElement("td");
            td.textContent = value;
            tr.append(td);
          }
          return tr;
        }),
      );
    };

    // 이 데모 컨테이너에 붙인다. document 에 붙이면 다른 데모의 이벤트까지 잡는다.
    sortTable.addEventListener("ns-sort", (e) => {
      drawSort(e.detail.key, e.detail.direction);
      sortLog.textContent =
        `ns-sort  key=${e.detail.key || "(없음)"}  direction=${e.detail.direction}`;
    });
  }
```

`innerHTML` 대신 `createElement`/`textContent` 를 쓴다. 데모 데이터가 정적이라 위험은 없지만, 문서가 가르치는 코드에 문자열 HTML 조립을 넣지 않는다.

- [ ] **Step 8: `docs/consumer-example.tsx` 에 `NsTable` 을 넣는다**

import 에 `NsTable` 과 타입을 추가하고, `Card` 안에 넣는다. **`e.detail` 을 실제로 읽어야** `EventName<>` 캐스트가 검사된다.

```tsx
import type { NsDialogCloseReason, NsSortDetail } from "../src/react/index.js";
```

```tsx
  const [sort, setSort] = useState<NsSortDetail>({ key: "", direction: "none" });
```

```tsx
            <NsTable
              sortKey={sort.key}
              sortDirection={sort.direction}
              onNsSort={(e) => setSort({ key: e.detail.key, direction: e.detail.direction })}
            >
              <table className="ns-table">
                <thead>
                  <tr>
                    <th data-ns-sort-key="name">
                      <button className="ns-table-sort" type="button">이름</button>
                    </th>
                  </tr>
                </thead>
                <tbody>
                  <tr><td>{sort.direction}</td></tr>
                </tbody>
              </table>
            </NsTable>
```

- [ ] **Step 9: `npm run check` 를 돌린다**

Run: `npm run check`
Expected: 통과. ③이 `ns-dialog-close, ns-navigate, ns-sort, ns-toggle` 넷을 보고한다.

- [ ] **Step 10: `EventName<>` 캐스트를 빼서 실패를 확인한다**

`src/react/elements.ts` 의 `onNsSort` 에서 `as EventName<...>` 을 지운다. `NsSortDetail` 타입 import 가 미사용이 되어 다른 이유로 먼저 실패할 수 있으므로, 그 경우 import 도 함께 지우고 두 `tsc` 를 **따로** 돌린다.

```sh
npx tsc -p tsconfig.json --pretty false          # 통과해야 한다
npx tsc -p tsconfig.consumer.json --pretty false # 실패해야 한다
```

Expected: **①은 통과하고 ②만 실패**한다. 실패 메시지가 `docs/consumer-example.tsx` 의 `e.detail` 을 가리키는지 확인한다. 라이브러리 안에서는 매핑 값이 그냥 문자열이라 ①이 못 본다는 것이 요점이다. 확인 후 되돌린다.

- [ ] **Step 11: 빌드하고 확인한다**

Run: `npm run build`

```sh
grep -c 'ns-table' dist/bundle.umd.js         # 1 이상 — register 가 번들에 남았는지
grep -c 'ns-sort' dist/index.js               # 1 이상
grep -c 'createRenderRoot' dist/index.js      # 1 이상 — Light DOM 재정의가 살아있는지
ls dist/components/table/ns-table.d.ts
```

- [ ] **Step 12: `createRenderRoot` 재정의를 지워 무엇이 깨지는지 확인한다**

이 컴포넌트가 존재하는 방식 자체를 확인하는 단계다. `createRenderRoot` 재정의를 임시로 주석 처리하고 빌드한다.

Run: `npm run build`

**`npm run check` 는 통과한다.** 타입 오류가 아니기 때문이다. 이것이 이 실패가 위험한 이유이고, **확인은 사람 몫이다** — 아래 Step 13 에 적는다. 확인 후 되돌린다.

- [ ] **Step 13: 사람이 확인할 것을 보고서에 적는다**

- **정렬 헤더를 Tab 으로 도달해 Enter/Space 로 정렬되는지** — `<button>` 을 요구한 이유
- 삼각형이 오름/내림에 따라 뒤집히고, 정렬 안 된 칼럼에는 안 보이지만 **자리를 차지해 헤더 폭이 흔들리지 않는지**
- 같은 헤더를 세 번 눌러 **원래 순서로 돌아오는지** (3-상태)
- "부서" 헤더는 눌러도 아무 일이 없는지
- 화면낭독기가 정렬 방향을 **한 번만** 안내하는지 (`border` 삼각형이라 텍스트가 없다)
- **`createRenderRoot` 재정의를 지우면 표가 사라지는지** (Step 12) — 검사가 통과하므로 눈으로만 드러난다

- [ ] **Step 14: 커밋**

```bash
git add -A
git commit -m "feat(table): ns-table 정렬 추가"
```

---

### Task 3: `ns-table` — 선택

Task 2 가 만든 클래스에 선택 기능을 더한다. 컴포넌트가 소유하는 것은 **전체 선택 체크박스 하나의 3-상태**다.

**Files:**
- Modify: `src/components/table/ns-table.ts`
- Modify: `src/types.ts`
- Modify: `src/index.ts`
- Modify: `src/react/elements.ts`
- Modify: `src/react/index.ts`
- Modify: `index.html` (섹션 확장 · 헬퍼 배선)
- Modify: `docs/consumer-example.tsx`

**Interfaces:**
- Consumes: `NsTable` 클래스 (Task 2) — `connectedCallback` · `disconnectedCallback` · `updated()` 를 확장한다. `.ns-checkbox` (기존 클래스 레이어)
- Produces:
  - `src/types.ts` → `export interface NsSelectChangeDetail { ids: string[] }`
  - `NsTable` 에 `selected?: string[]` 프로퍼티
  - `src/react/elements.ts` → `onNsSelectChange` 매핑

- [ ] **Step 1: `src/types.ts` 에 타입을 추가한다**

`NsSortDetail` **뒤**에 넣는다.

```ts
/**
 * ns-table 의 선택 변경. **요청되는 다음 전체 집합**이다 — 바뀐 하나가 아니다.
 *
 * 전체 집합으로 두는 이유는 소비자 처리가 한 줄이 되기 때문이다. 행 토글이든
 * 전체 선택이든 같다 — `setSelected(new Set(e.detail.ids))`.
 *
 * 범위는 **DOM 에 있는 행**, 즉 현재 페이지다. 서버 페이징에서 "전체 선택" 은
 * 보이는 20개를 뜻한다. "240개 전부 선택" 은 별개 기능이다.
 */
export interface NsSelectChangeDetail {
  ids: string[];
}
```

`declare global` 블록에 한 줄 추가한다.

```ts
    "ns-select-change": CustomEvent<NsSelectChangeDetail>;
```

- [ ] **Step 2: `ns-table.ts` 의 import 와 프로퍼티를 넓힌다**

```ts
import type {
  NsSelectChangeDetail,
  NsSortDetail,
  NsSortDirection,
} from "../../types.js";
```

`defaultSortDirection` 프로퍼티 **뒤**에 넣는다.

```ts
  /**
   * 제어 모드의 선택 집합. `undefined` 면 비제어다.
   *
   * 배열은 속성으로 표현할 수 없어 프로퍼티 전용이다. **비제어 초기 선택은
   * 마크업의 `checked` 속성에서 온다** — `default-selected` 프로퍼티를 두지
   * 않는다. 네이티브 폼과 같은 방식이고 컴포넌트는 DOM 을 읽으면 된다.
   */
  @property({ attribute: false }) selected?: string[];
```

`#innerDirection` 뒤에 필드를 추가한다.

```ts
  #observer?: MutationObserver;
```

- [ ] **Step 3: 생명주기를 확장한다**

`connectedCallback` 에 두 줄을 더한다.

```ts
    this.addEventListener("change", this.#onChange);

    /*
      행이 오고 가면(페이지 이동, 필터) 전체 선택의 3-상태를 다시 계산해야 한다.

      ns-field 를 light DOM 엘리먼트로 만드는 안은 MutationObserver 복잡도 때문에
      거절했다(프리미티브 스펙 §4.4). 여기서는 받아들인다 — 그 관찰자는 임의의
      자식에 id·aria-* 를 주입하는 일을 다시 해야 했고, 이 관찰자는
      querySelectorAll 로 개수를 다시 세는 한 가지만 한다.

      #syncSelectAll 은 checked·indeterminate 프로퍼티만 쓴다. 속성을 바꾸지
      않으므로 childList 관찰을 재발동시키지 않는다 — 무한 루프가 없다.
    */
    this.#observer = new MutationObserver(() => this.#syncSelectAll());
    this.#observer.observe(this, { childList: true, subtree: true });
```

`disconnectedCallback` 에 두 줄을 더한다.

```ts
    this.removeEventListener("change", this.#onChange);
    this.#observer?.disconnect();
```

`updated()` 를 넓힌다.

```ts
  protected override updated(): void {
    this.#syncAriaSort();
    this.#syncSelectAll();
  }
```

- [ ] **Step 4: 선택 로직을 추가한다**

`#onClick` **뒤**에 넣는다.

```ts
  #rowBoxes(): HTMLInputElement[] {
    return [...this.querySelectorAll<HTMLInputElement>("input[data-ns-row-id]")];
  }

  #rowId(box: HTMLInputElement): string {
    return box.dataset.nsRowId ?? "";
  }

  /*
    전체 선택 체크박스의 3-상태를 쓴다. checked 와 indeterminate 의 유일한
    작성자가 컴포넌트다 — 소비자는 그 둘을 바인딩하지 않는다.

    indeterminate 는 프로퍼티고 대응하는 HTML 속성이 없다. 마크업만으로는
    "일부 선택" 을 만들 수 없어서, 이것이 컴포넌트가 가져갈 값이 있는 지점이다.
  */
  #syncSelectAll(): void {
    const all = this.querySelector<HTMLInputElement>("input[data-ns-select-all]");
    if (!all) return;

    const boxes = this.#rowBoxes();
    const selected = this.selected;
    const count =
      selected === undefined
        ? boxes.filter((box) => box.checked).length
        : boxes.filter((box) => selected.includes(this.#rowId(box))).length;

    all.checked = boxes.length > 0 && count === boxes.length;
    all.indeterminate = count > 0 && count < boxes.length;
  }

  #emitSelect(ids: string[]): void {
    const detail: NsSelectChangeDetail = { ids };
    this.dispatchEvent(
      new CustomEvent("ns-select-change", { detail, bubbles: true, composed: true }),
    );
  }

  #onChange = (e: Event): void => {
    const box = (e.target as Element | null)?.closest<HTMLInputElement>(
      'input[type="checkbox"]',
    );
    if (!box) return;

    const boxes = this.#rowBoxes();

    if (box.hasAttribute("data-ns-select-all")) {
      /*
        비제어면 행 체크박스를 직접 쓴다 — 아무도 안 하니까. 제어면 쓰지 않는다:
        React 가 checked 를 소유하므로 다음 렌더에 덮어쓴다.
      */
      if (this.selected === undefined) {
        for (const row of boxes) row.checked = box.checked;
      }
      this.#emitSelect(box.checked ? boxes.map((row) => this.#rowId(row)) : []);
      // 제어 모드에서는 selected 가 아직 옛 값이다. updated() 가 갱신한다.
      if (this.selected === undefined) this.#syncSelectAll();
      return;
    }

    if (!box.hasAttribute("data-ns-row-id")) return;

    let ids: string[];
    if (this.selected === undefined) {
      ids = boxes.filter((row) => row.checked).map((row) => this.#rowId(row));
      this.#emitSelect(ids);
      this.#syncSelectAll();
      return;
    }

    /*
      제어 모드에서는 다른 행의 DOM checked 를 신뢰할 수 없다 — React 가 아직
      리렌더하지 않았을 수 있다. 방금 눌린 하나의 변화만 selected 에 반영하고,
      순서는 DOM 의 행 순서를 따른다.
    */
    const next = new Set(this.selected);
    const id = this.#rowId(box);
    if (box.checked) next.add(id);
    else next.delete(id);
    ids = boxes.map((row) => this.#rowId(row)).filter((rowId) => next.has(rowId));

    this.#emitSelect(ids);
  };
```

- [ ] **Step 5: `src/types.ts` 재export 와 래퍼를 넓힌다**

`src/index.ts` 의 타입 재export 에 `NsSelectChangeDetail` 을 추가한다.

`src/react/elements.ts` 의 타입 import 에 `NsSelectChangeDetail` 을 추가하고, `NsTable` 래퍼의 `events` 를 넓힌다.

```ts
  events: {
    // EventName<> 브랜딩이 없으면 핸들러가 (e: Event) => void 로 타입된다.
    onNsSort: "ns-sort" as EventName<CustomEvent<NsSortDetail>>,
    onNsSelectChange: "ns-select-change" as EventName<CustomEvent<NsSelectChangeDetail>>,
  },
```

`src/react/index.ts` 의 타입 재export 에 `NsSelectChangeDetail` 을 추가한다.

- [ ] **Step 6: `index.html` 의 `ns-table` 절에 선택 데모와 표를 추가한다**

`<h3>프로퍼티</h3>` **앞**에 데모를 넣는다.

```html
  <h3>선택 데모</h3>
  <template class="ex">
    <ns-table>
      <table class="ns-table">
        <thead>
          <tr>
            <th><label class="ns-checkbox"><input type="checkbox" data-ns-select-all></label></th>
            <th>이름</th>
            <th>부서</th>
          </tr>
        </thead>
        <tbody>
          <tr><td><label class="ns-checkbox"><input type="checkbox" data-ns-row-id="hong"></label></td><td>홍길동</td><td>마케팅팀</td></tr>
          <tr><td><label class="ns-checkbox"><input type="checkbox" data-ns-row-id="kim"></label></td><td>김대기</td><td>플랫폼개발팀</td></tr>
          <tr><td><label class="ns-checkbox"><input type="checkbox" data-ns-row-id="park"></label></td><td>박서준</td><td>데이터분석팀</td></tr>
        </tbody>
      </table>
    </ns-table>
  </template>
  <div class="demo block" id="select-demo" style="padding: var(--space-4)"></div>
  <pre></pre>
  <p id="select-log" style="font-family: monospace">체크하면 여기에 이벤트가 찍힌다.</p>
  <p>
    한 행만 체크하면 전체 선택이 <strong>가로줄(indeterminate)</strong> 이 된다.
    <code>indeterminate</code> 는 프로퍼티고 대응하는 HTML 속성이 없어
    <strong>마크업만으로는 만들 수 없다</strong> — 컴포넌트가 이걸 맡는 이유다.
  </p>
```

`<h3>소비자가 쓰는 훅</h3>` 표에 두 행을 추가한다.

```html
    <tr><td><code>data-ns-select-all</code></td><td><code>input[type=checkbox]</code></td><td>전체 선택. <strong><code>checked</code>·<code>indeterminate</code> 를 컴포넌트가 쓴다</strong> — 바인딩하지 않는다</td></tr>
    <tr><td><code>data-ns-row-id</code></td><td><code>input[type=checkbox]</code></td><td>행 식별자. 값이 <code>ns-select-change</code> 의 <code>ids</code> 에 실린다</td></tr>
```

`<h3>프로퍼티</h3>` 표에 한 행을 추가한다.

```html
    <tr><td><code>selected</code></td><td>없음</td><td>string[] | undefined</td><td><code>undefined</code></td><td>제어 모드. <strong>비제어 초기 선택은 마크업의 <code>checked</code> 에서 온다</strong></td></tr>
```

`<h3>이벤트</h3>` 표에 한 행을 추가한다.

```html
    <tr><td><code>ns-select-change</code></td><td><code>{ ids: string[] }</code></td><td>체크박스 변경. <strong>요청되는 다음 전체 집합</strong>이다</td></tr>
```

`<h3>접근성</h3>` **앞**에 절을 하나 넣는다.

```html
  <h3>전체 선택의 범위는 현재 페이지다</h3>
  <p>
    DOM 에 있는 행만 대상이다. 서버 페이징에서 "전체 선택" 은 <strong>보이는 20개</strong>를
    뜻한다. "240개 전부 선택" 은 별개 기능으로 제공하지 않는다.
  </p>
  <p>
    제어 모드에서는 <strong>행 체크박스의 <code>checked</code> 를 컴포넌트가 건드리지
    않는다.</strong> React 가 그 값을 소유하므로 쓰면 다음 렌더에 덮어써진다. 요청되는
    다음 집합만 이벤트로 올리고, 적용은 소비자가 한다.
  </p>
```

- [ ] **Step 7: `index.html` 헬퍼에 선택 데모 배선을 추가한다**

정렬 데모 배선 **다음**에 넣는다.

```js
  const selectDemo = document.getElementById("select-demo");
  const selectLog = document.getElementById("select-log");
  if (selectDemo && selectLog) {
    // 비제어 데모다 — 컴포넌트가 행 체크박스와 3-상태를 모두 관리한다.
    selectDemo.querySelector("ns-table").addEventListener("ns-select-change", (e) => {
      const ids = e.detail.ids;
      selectLog.textContent = `ns-select-change  ids=[${ids.join(", ")}]  (${ids.length}개)`;
    });
  }
```

- [ ] **Step 8: `docs/consumer-example.tsx` 를 넓힌다**

`selected` 를 제어 모드로 쓰고 `e.detail.ids` 를 읽는다.

```tsx
  const [rows, setRows] = useState<string[]>([]);
```

Task 2 가 넣은 `<NsTable>` 을 이렇게 확장한다.

```tsx
            <NsTable
              sortKey={sort.key}
              sortDirection={sort.direction}
              selected={rows}
              onNsSort={(e) => setSort({ key: e.detail.key, direction: e.detail.direction })}
              onNsSelectChange={(e) => setRows(e.detail.ids)}
            >
              <table className="ns-table">
                <thead>
                  <tr>
                    <th>
                      <label className="ns-checkbox">
                        <input type="checkbox" data-ns-select-all />
                      </label>
                    </th>
                    <th data-ns-sort-key="name">
                      <button className="ns-table-sort" type="button">이름</button>
                    </th>
                  </tr>
                </thead>
                <tbody>
                  <tr>
                    <td>
                      <label className="ns-checkbox">
                        <input type="checkbox" data-ns-row-id="a" checked={rows.includes("a")} readOnly />
                      </label>
                    </td>
                    <td>{sort.direction}</td>
                  </tr>
                </tbody>
              </table>
            </NsTable>
```

`readOnly` 를 붙이는 이유는 React 가 `checked` 만 있고 `onChange` 가 없는 입력에 경고를 내기 때문이다. 변경은 `ns-select-change` 위임으로 처리되므로 `onChange` 를 각 행에 붙이지 않는다.

- [ ] **Step 9: `npm run check` 를 돌린다**

Run: `npm run check`
Expected: 통과. ③이 `ns-dialog-close, ns-navigate, ns-select-change, ns-sort, ns-toggle` 다섯을 보고한다.

- [ ] **Step 10: `EventName<>` 캐스트를 빼서 실패를 확인한다**

`onNsSelectChange` 의 캐스트를 지운다. 미사용 import 가 다른 이유로 먼저 실패하면 그것도 지우고 두 `tsc` 를 따로 돌린다.

```sh
npx tsc -p tsconfig.json --pretty false          # 통과해야 한다
npx tsc -p tsconfig.consumer.json --pretty false # 실패해야 한다 — e.detail.ids 를 가리켜야 한다
```

확인 후 되돌린다.

- [ ] **Step 11: 빌드하고 확인한다**

Run: `npm run build`

```sh
grep -c 'ns-select-change' dist/index.js     # 1 이상
grep -c 'indeterminate' dist/index.js        # 1 이상
grep -c 'MutationObserver' dist/index.js     # 1 이상
```

- [ ] **Step 12: 사람이 확인할 것을 보고서에 적는다**

- 한 행만 체크했을 때 전체 선택이 **가로줄**로 보이는지
- 전체 선택을 누르면 세 행이 모두 체크되고, 다시 누르면 모두 해제되는지
- 세 행을 손으로 다 체크하면 전체 선택이 **가로줄이 아니라 완전 체크**로 바뀌는지
- 로그의 `ids` 가 **DOM 행 순서**를 따르는지
- 정렬 데모에서 헤더를 눌러 행이 다시 그려진 뒤에도 전체 선택 상태가 맞는지 (`MutationObserver`)
- 체크박스 라벨 영역을 클릭해도 토글되는지 (`.ns-checkbox` 의 암묵적 label)

- [ ] **Step 13: 커밋**

```bash
git add -A
git commit -m "feat(table): ns-table 선택과 전체 선택 3-상태 추가"
```

---

### Task 4: `ns-pagination`

렌더하는 Light DOM 컴포넌트다. `ns-table` 과 달리 자식이 없어 Lit 이 그 안에 렌더해도 덮어쓸 것이 없다.

**Files:**
- Create: `src/components/pagination/ns-pagination.ts`
- Modify: `src/controls/controls.css`
- Modify: `src/types.ts`
- Modify: `src/index.ts`
- Modify: `src/react/elements.ts`
- Modify: `src/react/index.ts`
- Modify: `index.html` (nav 항목 · 섹션 · 헬퍼 배선)
- Modify: `docs/consumer-example.tsx`

**Interfaces:**
- Consumes: `.ns-button` · `.ns-button--ghost` · `.ns-button--outline` · `.ns-button--sm` (기존 클래스 레이어)
- Produces:
  - `src/types.ts` → `export interface NsPageChangeDetail { page: number }`
  - `export class NsPagination extends LitElement` with `total: number`, `perPage: number`, `page?: number`, `defaultPage: number`
  - `export function pageWindow(current: number, total: number): (number | "gap")[]`
  - 클래스 `.ns-pagination-gap`, 요소 선택자 `ns-pagination`

- [ ] **Step 1: `src/types.ts` 에 타입을 추가한다**

```ts
/** ns-pagination 의 페이지 이동. 요청되는 다음 페이지다. */
export interface NsPageChangeDetail {
  page: number;
}
```

`declare global` 에 한 줄 추가한다.

```ts
    "ns-page-change": CustomEvent<NsPageChangeDetail>;
```

- [ ] **Step 2: `src/components/pagination/ns-pagination.ts` 를 만든다**

```ts
import { LitElement, html, nothing } from "lit";
import { property } from "lit/decorators.js";

import { register } from "../../internal/register.js";
import { warnIfTokensMissing } from "../../internal/warn-missing-tokens.js";
import type { NsPageChangeDetail } from "../../types.js";

/**
 * 번호 윈도우.
 *
 * - 페이지 수 ≤ 7 이면 전부
 * - 그 외에는 첫 페이지 · 현재±1 · 마지막 페이지, 빈 구간에 `"gap"`
 *
 * 내보내는 이유는 규칙이 모호하지 않게 문서화되기 위해서다. 소비자가 쓸 API 는
 * 아니고 `src/index.ts` 에서 재export 하지 않는다.
 */
export function pageWindow(current: number, total: number): (number | "gap")[] {
  if (total <= 7) return Array.from({ length: total }, (_, i) => i + 1);

  const wanted = [1, current - 1, current, current + 1, total]
    .filter((page) => page >= 1 && page <= total)
    .sort((a, b) => a - b);

  const out: (number | "gap")[] = [];
  let previous = 0;
  for (const page of wanted) {
    if (page === previous) continue;
    if (previous !== 0 && page - previous > 1) out.push("gap");
    out.push(page);
    previous = page;
  }
  return out;
}

/**
 * 목록 페이지 이동 컨트롤. 표를 모르고 데이터를 모른다 — 어떤 목록에도 붙는다.
 */
export class NsPagination extends LitElement {
  /*
    Light DOM 이다. controls.css 의 .ns-button 을 그대로 쓰기 위해서다 — shadow
    였다면 버튼 스타일 전부를 다시 적어야 했고, ns-dialog 닫기 버튼에서 수용한
    중복(열 줄)이 여기서는 훨씬 커진다.

    ns-table 과 달리 자식이 없으므로 Lit 이 이 요소 안에 렌더해도 덮어쓸 소비자
    내용이 없다. 그래서 LitElement 를 그대로 쓴다.

    static styles 는 이 재정의로 무시된다 — adoptStyles 가 호출되지 않는다.
    스타일은 전부 controls.css 에 있고 이 컴포넌트에 .styles.ts 가 없다.
  */
  protected override createRenderRoot(): HTMLElement {
    return this;
  }

  /** 전체 **항목** 수. 페이지 수가 아니다 — 서버 응답이 주는 것이 보통 이쪽이다. */
  @property({ type: Number }) total = 0;

  @property({ type: Number, attribute: "per-page" }) perPage = 20;

  /**
   * 제어 모드의 현재 페이지. `undefined` 면 비제어다.
   *
   * 속성이 아니라 프로퍼티 전용인 이유는 ns-dialog 의 `open` 과 같다 —
   * `<ns-pagination page="3">` 이라고 쓰면 제어 모드로 들어가 컴포넌트가 스스로
   * 페이지를 넘기지 못한다. 순수 HTML 은 `default-page` 를 쓴다.
   */
  @property({ attribute: false }) page?: number;

  /** 비제어 초기 페이지. */
  @property({ type: Number, attribute: "default-page" }) defaultPage = 1;

  #innerPage = 1;
  #warned = false;

  get #controlled(): boolean {
    return this.page !== undefined;
  }

  get #pages(): number {
    return Math.ceil(this.total / this.perPage);
  }

  override connectedCallback(): void {
    super.connectedCallback();
    warnIfTokensMissing();
  }

  /*
    비제어 초기값을 seed 한다. 덮어쓰지 않는다 — Lit 은 첫 업데이트를
    마이크로태스크로 미루므로, 생성과 같은 태스크에서 프로퍼티를 만진 코드가
    여기보다 먼저 실행된다.
  */
  protected override firstUpdated(): void {
    if (this.defaultPage !== 1) this.#innerPage = this.defaultPage;
  }

  #current(): number {
    const raw = this.page ?? this.#innerPage;
    const pages = this.#pages;
    if (raw >= 1 && raw <= pages) return raw;

    /*
      범위를 벗어난 page 는 표시용으로만 clamp 하고 경고를 한 번 낸다.
      이벤트로 교정하지 않는다 — 소비자 상태와 서로 밀어내는 루프가 된다.
    */
    if (!this.#warned) {
      this.#warned = true;
      console.warn(
        `[ns-pagination] page=${raw} 가 1..${pages} 범위를 벗어났습니다.`,
      );
    }
    return Math.min(Math.max(raw, 1), Math.max(pages, 1));
  }

  #go(page: number): void {
    // 현재 페이지 클릭은 아무 일도 하지 않는다.
    if (page === this.#current()) return;

    if (!this.#controlled) {
      this.#innerPage = page;
      this.requestUpdate();
    }

    const detail: NsPageChangeDetail = { page };
    this.dispatchEvent(
      new CustomEvent("ns-page-change", { detail, bubbles: true, composed: true }),
    );
  }

  protected override render() {
    const pages = this.#pages;
    // 페이지가 하나 이하면 쓸모없는 컨트롤을 남기지 않는다.
    if (pages <= 1) return nothing;

    const current = this.#current();

    return html`
      <nav aria-label="페이지 이동">
        <button
          class="ns-button ns-button--ghost ns-button--sm"
          type="button"
          ?disabled=${current === 1}
          @click=${() => this.#go(current - 1)}
        >
          이전
        </button>
        ${pageWindow(current, pages).map((entry) =>
          entry === "gap"
            ? html`<span class="ns-pagination-gap" aria-hidden="true">…</span>`
            : html`<button
                class=${entry === current
                  ? "ns-button ns-button--outline ns-button--sm"
                  : "ns-button ns-button--ghost ns-button--sm"}
                type="button"
                aria-current=${entry === current ? "page" : nothing}
                @click=${() => this.#go(entry)}
              >
                ${entry}
              </button>`,
        )}
        <button
          class="ns-button ns-button--ghost ns-button--sm"
          type="button"
          ?disabled=${current === pages}
          @click=${() => this.#go(current + 1)}
        >
          다음
        </button>
      </nav>
    `;
  }
}

register("ns-pagination", NsPagination);

declare global {
  interface HTMLElementTagNameMap {
    "ns-pagination": NsPagination;
  }
}
```

**현재 페이지 버튼을 `disabled` 로 하지 않는다.** 탭 순서에서 빠지면 키보드 사용자가 위치를 잃는다. `aria-current="page"` 로 표시하고 클릭은 `#go` 가 무시한다.

- [ ] **Step 3: `controls.css` 에 페이징 스타일을 추가한다**

`.ns-table-sort` 규칙 **다음**, `.ns-card` **앞**에 넣는다.

```css
  ns-pagination {
    display: block;
  }

  /*
    nav 는 이 컴포넌트가 렌더하는 유일한 자식이라 요소 타입으로 특정된다 —
    클래스를 붙이지 않는다.
  */
  ns-pagination nav {
    display: flex;
    align-items: center;
    gap: var(--space-1);
  }

  .ns-pagination-gap {
    padding-inline: var(--space-2);
    color: var(--color-fg-subtle);
    font-size: var(--font-size-sm);
    line-height: var(--line-height-sm);
  }
```

- [ ] **Step 4: 등록·래퍼·재export 를 추가한다**

`src/index.ts`:

```ts
import "./components/pagination/ns-pagination.js";
```

```ts
export { NsPagination } from "./components/pagination/ns-pagination.js";
```

타입 재export 에 `NsPageChangeDetail` 을 추가한다.

`src/react/elements.ts`:

```ts
import { NsPagination as NsPaginationElement } from "../components/pagination/ns-pagination.js";
```

```ts
export const NsPagination = createComponent({
  react: React,
  tagName: "ns-pagination",
  elementClass: NsPaginationElement,
  events: {
    // EventName<> 브랜딩이 없으면 핸들러가 (e: Event) => void 로 타입된다.
    onNsPageChange: "ns-page-change" as EventName<CustomEvent<NsPageChangeDetail>>,
  },
});
```

`src/react/index.ts` 의 태그 export 줄에 `NsPagination` 을 알파벳 위치에 넣고, 타입 재export 에 `NsPageChangeDetail` 을 추가한다.

- [ ] **Step 5: `index.html` 에 항목과 섹션을 추가한다**

"컴포넌트" 그룹의 `ns-table` 항목 **다음**에 넣는다.

```html
      <ns-nav-item href="#ns-pagination" label="ns-pagination" badge="PG"></ns-nav-item>
```

`ns-table` 섹션 **다음**, `<h2 id="ns-header">` **앞**에 넣는다.

```html
  <h2 id="ns-pagination">ns-pagination</h2>
  <p>
    목록 페이지 이동 컨트롤. <strong>표를 모르고 데이터를 모른다</strong> — 어떤 목록에도 붙는다.
    페이지를 실제로 바꾸는 것은 소비자다.
  </p>
  <p>
    <strong>Light DOM 이라 <code>.ns-button</code> 을 그대로 쓴다.</strong> shadow 였다면
    버튼 스타일 전부를 다시 적어야 했다 — <code>ns-dialog</code> 닫기 버튼에서 수용한
    열 줄짜리 중복이 여기서는 훨씬 커진다.
  </p>

  <h3>데모</h3>
  <template class="ex">
    <ns-pagination total="240" per-page="20" default-page="4"></ns-pagination>
  </template>
  <div class="demo block" id="pagination-demo" style="padding: var(--space-4)"></div>
  <pre></pre>
  <p id="pagination-log" style="font-family: monospace">페이지를 누르면 여기에 이벤트가 찍힌다.</p>

  <h3>경계</h3>
  <template class="ex">
    <div style="display:grid;gap:var(--space-4)">
      <ns-pagination total="60" per-page="20" default-page="1"></ns-pagination>
      <ns-pagination total="140" per-page="20" default-page="7"></ns-pagination>
      <ns-pagination total="15" per-page="20"></ns-pagination>
    </div>
  </template>
  <div class="demo block" id="pagination-edge-demo" style="padding: var(--space-4)"></div>
  <pre></pre>
  <p>
    위에서부터 3페이지(전부 표시) · 7페이지(<code>…</code> 등장) · 1페이지
    (<strong>아무것도 렌더하지 않는다</strong>) 다.
  </p>

  <h3>프로퍼티</h3>
  <table class="ns-table">
    <tr><th>프로퍼티</th><th>속성</th><th>타입</th><th>기본값</th><th>설명</th></tr>
    <tr><td><code>total</code></td><td><code>total</code></td><td>number</td><td><code>0</code></td><td>전체 <strong>항목</strong> 수. 페이지 수가 아니다</td></tr>
    <tr><td><code>perPage</code></td><td><code>per-page</code></td><td>number</td><td><code>20</code></td><td></td></tr>
    <tr><td><code>page</code></td><td>없음</td><td>number | undefined</td><td><code>undefined</code></td><td>제어 모드. <strong>속성이 아니라 프로퍼티다</strong></td></tr>
    <tr><td><code>defaultPage</code></td><td><code>default-page</code></td><td>number</td><td><code>1</code></td><td>비제어 초기값</td></tr>
  </table>

  <h3>이벤트</h3>
  <table class="ns-table">
    <tr><th>이름</th><th>detail</th><th>발생 시점</th></tr>
    <tr><td><code>ns-page-change</code></td><td><code>{ page: number }</code></td><td>다른 페이지 클릭. 현재 페이지를 눌러도 발생하지 않는다</td></tr>
  </table>

  <h3>클래스</h3>
  <table class="ns-table">
    <tr><th>이름</th><th>붙이는 요소</th><th>설명</th></tr>
    <tr><td><code>ns-pagination</code></td><td>(요소 선택자)</td><td>배치. 클래스가 아니라 태그 자체를 스타일한다</td></tr>
    <tr><td><code>.ns-pagination-gap</code></td><td>컴포넌트가 렌더하는 <code>span</code></td><td><code>…</code> 자리. 나머지 버튼은 전부 <code>.ns-button</code> 재사용이다</td></tr>
  </table>
  <p>
    <code>nav</code> 는 이 컴포넌트가 렌더하는 유일한 자식이라 요소 타입으로 특정되어
    클래스가 없다. 이 표에 <code>.ns-pagination-gap</code> 이 적혀 있어야
    <code>check-controls.mjs</code> 가 통과한다 — 데모 템플릿에는 그 클래스가 소스로
    등장하지 않는다(컴포넌트가 런타임에 만든다).
  </p>

  <h3>번호 윈도우</h3>
  <ul>
    <li>페이지 수 ≤ 7 이면 전부 표시</li>
    <li>그 외에는 <strong>첫 페이지 · 현재±1 · 마지막 페이지</strong>, 빈 구간에 <code>…</code></li>
    <li>현재 페이지는 <code>--outline</code> + <code>aria-current="page"</code>. <strong>비활성화하지 않는다</strong> — 탭 순서에서 빠지면 키보드 사용자가 위치를 잃는다</li>
    <li>이전·다음은 양 끝에서 <code>disabled</code></li>
  </ul>

  <h3>경계 처리</h3>
  <ul>
    <li><strong>페이지가 1개 이하면 아무것도 렌더하지 않는다.</strong> <code>total = 0</code> 도 같다 — 빈 상태 문구는 소비자 것이다</li>
    <li><code>page</code> 가 범위를 벗어나면 표시용으로만 clamp 하고 콘솔 경고를 한 번 낸다. <strong>이벤트로 교정하지 않는다</strong> — 소비자 상태와 서로 밀어내는 루프가 된다</li>
  </ul>

  <h3>제공하지 않는 것</h3>
  <p>
    페이지 크기 선택(10/20/50)과 "전체 240개 선택" 은 없다. 전자는 컨트롤이 하나 더
    늘고 지금 요구가 없으며, 후자는 <code>ns-table</code> 의 선택 범위 결정과 짝인 별개 기능이다.
  </p>

  <h3>HTML</h3>
  <script type="text/plain">
    <ns-pagination total="240" per-page="20" default-page="1"></ns-pagination>
  </script>

  <h3>HTML — 배선</h3>
  <script type="text/plain">
    document.querySelector("ns-pagination").addEventListener("ns-page-change", (e) => {
      load(e.detail.page);   /* 데이터를 가져오는 것은 소비자다 */
    });
  </script>

  <h3>React</h3>
  <script type="text/plain">
    import { NsPagination } from "@neosimplix/common-ui/react";

    const [page, setPage] = useState(1);

    <NsPagination
      total={total}
      perPage={20}
      page={page}
      onNsPageChange={(e) => setPage(e.detail.page)}
    />
  </script>
```

- [ ] **Step 6: `index.html` 헬퍼에 페이징 데모 배선을 추가한다**

선택 데모 배선 **다음**에 넣는다.

```js
  const paginationDemo = document.getElementById("pagination-demo");
  const paginationLog = document.getElementById("pagination-log");
  if (paginationDemo && paginationLog) {
    // 비제어 데모다 — 컴포넌트가 현재 페이지를 스스로 관리한다.
    paginationDemo.querySelector("ns-pagination").addEventListener("ns-page-change", (e) => {
      paginationLog.textContent = `ns-page-change  page=${e.detail.page}`;
    });
  }
```

경계 데모에는 리스너를 붙이지 않는다 — 렌더 결과를 보는 자리다.

- [ ] **Step 7: `docs/consumer-example.tsx` 에 `NsPagination` 을 넣는다**

```tsx
  const [page, setPage] = useState(1);
```

```tsx
            <NsPagination
              total={240}
              perPage={20}
              page={page}
              onNsPageChange={(e) => setPage(e.detail.page)}
            />
```

- [ ] **Step 8: `npm run check` 를 돌린다**

Run: `npm run check`
Expected: 통과. ③이 여섯을 보고한다 — `ns-dialog-close, ns-navigate, ns-page-change, ns-select-change, ns-sort, ns-toggle`. ④의 목록에 `ns-pagination` · `ns-pagination-gap` 이 포함된다.

- [ ] **Step 9: `EventName<>` 캐스트를 빼서 실패를 확인한다**

`onNsPageChange` 의 캐스트를 지우고 두 `tsc` 를 따로 돌린다. ①은 통과, ②는 `e.detail.page` 를 가리키며 실패해야 한다. 확인 후 되돌린다.

- [ ] **Step 10: 윈도우 규칙을 손으로 추적해 보고서에 적는다**

`dist/index.js` 는 `lit` 을 external 로 두므로 Node 에서 직접 import 되지 않고, 이 저장소에는 테스트 러너가 없다. 그래서 **`pageWindow` 를 다섯 입력으로 손으로 추적해 결과를 보고서에 적는다.** 순수 함수라 추적이 확정적이다.

| current | total | 기대 결과 |
|---|---|---|
| 4 | 12 | `[1, "gap", 3, 4, 5, "gap", 12]` |
| 1 | 12 | `[1, 2, "gap", 12]` |
| 12 | 12 | `[1, "gap", 11, 12]` |
| 4 | 7 | `[1, 2, 3, 4, 5, 6, 7]` |
| 4 | 8 | `[1, "gap", 3, 4, 5, "gap", 8]` |

각 입력에 대해 `wanted` 배열(필터·정렬 후)과 루프의 `out` 누적을 적는다. 코드가 이 표와 다른 결과를 내면 **코드를 고치지 말고 보고한다** — 윈도우 규칙은 설계 문서 §7.3 이 정한 것이고, 어긋나면 어느 쪽이 틀렸는지 사람이 정해야 한다.

경계 데모(Step 5)가 3페이지 · 7페이지 · 1페이지를 화면으로 덮으므로, 이 추적과 그 데모가 서로를 교차 검증한다.

- [ ] **Step 11: 빌드하고 확인한다**

Run: `npm run build`

```sh
grep -c 'ns-pagination' dist/bundle.umd.js       # 1 이상
grep -c 'ns-page-change' dist/index.js           # 1 이상
grep -c 'aria-current' dist/index.js             # 1 이상
grep -c 'ns-pagination-gap' dist/controls.css    # 1 이상
```

- [ ] **Step 12: 사람이 확인할 것을 보고서에 적는다**

- 데모(240항목 · 20/페이지 · 4페이지)가 `1 … 3 4 5 … 12` 로 보이는지
- 현재 페이지가 `--outline` 으로 구분되고 **Tab 으로 도달 가능한지**
- 1페이지에서 "이전" 이, 마지막에서 "다음" 이 비활성인지
- 경계 데모 셋이 각각 전부 표시 · `…` 등장 · **아무것도 안 보임** 인지
- 버튼들이 `.ns-table`/`.ns-button` 과 같은 모양인지 (**Light DOM 재사용이 실제로 먹는지**)
- 콘솔에서 `el.page = 99` 를 준 뒤 경고가 한 번만 나고 표시가 clamp 되는지

- [ ] **Step 13: 커밋**

```bash
git add -A
git commit -m "feat(pagination): ns-pagination 추가"
```

---

### Task 5: 규칙 · 함정 · 구조 문서

**Files:**
- Modify: `.claude/rules/library-invariants.md`
- Modify: `docs/gotchas.md`
- Modify: `docs/project-structure.md`
- Modify: `.claude/skills/adding-a-component/SKILL.md`
- Modify: `.claude/skills/releasing/SKILL.md`

**Interfaces:**
- Consumes: Task 1~4 의 모든 결정
- Produces: 없음 (문서 전용)

- [ ] **Step 1: `library-invariants.md` 에 Light DOM 규칙을 추가한다**

"## 컴포넌트" 절에 넣는다.

```markdown
- **공통 컨트롤 스타일을 재사용해야 하는 컴포넌트는 Light DOM 을 쓴다.** shadow root 를 갖는 이유는 스타일 캡슐화 하나이고, `controls.css` 를 쓰려는 컴포넌트에는 그 캡슐화가 방해다. `createRenderRoot()` 를 재정의해 `this` 를 반환한다.
- **Light DOM 컴포넌트는 `static styles` 를 갖지 않는다.** `createRenderRoot` 재정의로 `adoptStyles` 가 호출되지 않아 조용히 무시된다. **"로직과 스타일을 파일 두 개로 나눈다" 규칙의 예외다** — 스타일이 전부 `controls.css` 에 있으므로 `.styles.ts` 파일이 없다.
- **소비자 자식을 품는 Light DOM 컴포넌트는 `ReactiveElement` 를 상속한다.** `LitElement` 는 템플릿을 렌더해 그 자식을 덮어쓴다. 렌더가 필요하고 자식이 없는 경우에만 `LitElement` + `createRenderRoot` 재정의를 쓴다.
- **소비자가 쓰는 훅 속성은 `data-ns-` 접두사를 쓴다.** Light DOM 이라 문서 이름공간에 들어가고, 충돌하면 엉뚱한 요소를 오인해 에러 없이 오동작한다.
```

"## 이름" 절에 한 줄을 더한다.

```markdown
- **컴포넌트 유형이 셋이다.** shadow + 렌더(기본), Light DOM + 렌더(`ns-pagination`), Light DOM + 렌더 없음(`ns-table`). 판단은 "캡슐화가 필요한가 / 소비자 자식을 품는가" 두 축이다.
```

- [ ] **Step 2: `docs/gotchas.md` 에 함정 셋을 추가한다**

"## 검사는 실패시켜 봐야 검사다" 절 **앞**에 넣는다.

```markdown
## Light DOM 에서 소비자 자식이 사라지는 두 경로

`ns-table` 은 소비자가 쓴 `<table>` 을 품는다. 그 자식이 사라지는 방법이 둘이고 서로 다르다.

**1. Lit 템플릿을 렌더하면 덮어써진다.** `createRenderRoot() { return this }` 로 light DOM 에 렌더하면 Lit 이 그 요소의 내용을 자기 템플릿으로 교체한다. 빈 템플릿이라도 그렇다.

**2. shadow root 가 생기면 가려진다.** `ReactiveElement` 의 **기본** `createRenderRoot()` 는 shadow root 를 만든다. shadow root 가 있으면 `<slot>` 이 없는 한 light DOM 자식이 렌더되지 않는다.

즉 `ReactiveElement` 를 상속하는 것만으로는 부족하다 — 그것은 1번만 막는다. `createRenderRoot` 재정의가 2번을 막는다.

**둘 다 에러 없이 빈 표가 된다.** `npm run check` 는 타입만 보므로 통과한다.

→ 소비자 자식을 품는 Light DOM 컴포넌트는 `ReactiveElement` 상속 **과** `createRenderRoot` 재정의 둘 다 한다. 자식이 없다면(`ns-pagination`) `LitElement` + 재정의로 충분하다.

## Light DOM 컴포넌트의 `static styles` 는 조용히 무시된다

`createRenderRoot` 를 재정의하면 Lit 의 `adoptStyles` 가 호출되지 않는다. `static styles` 를 적어도 아무 일도 일어나지 않고 경고도 없다.

→ Light DOM 컴포넌트에는 `.styles.ts` 파일을 만들지 않는다. 스타일은 `controls.css` 에 요소 선택자로 적는다. 그것이 오히려 이 컴포넌트들이 Light DOM 인 이유다 — `.ns-button` 을 재사용하려고 캡슐화를 포기한 것이다.

## `check-controls.mjs` 는 요소 선택자를 못 봤다

정규식이 `\.(ns-[a-z0-9_-]+)` 로 **점이 붙은 클래스만** 잡았다. `ns-table { overflow-x: auto }` 같은 요소 선택자는 그냥 빠졌다 — Light DOM 컴포넌트가 처음 들어오면서 드러났다.

정방향만 넓혔다. 역방향은 넣지 않는다 — 태그 이름은 `index.html` 전체에 정당하게 등장하므로(데모 마크업, 프로퍼티 표, 예시 블록) 역방향으로 보면 거짓 양성만 쏟아진다.

→ `controls.css` 에 새 규칙 형태가 들어올 때마다 이 검사가 그것을 보는지 확인한다. 검사가 못 보는 규칙은 문서 대조 밖에 있다.
```

- [ ] **Step 3: `docs/project-structure.md` 를 갱신한다**

태그 표에 두 줄을 추가한다.

```markdown
| `ns-table` | 소비자 `table` 을 감싸는 Light DOM. 정렬 상태 · 전체 선택 3-상태. **셀을 렌더하지 않는다** |
| `ns-pagination` | Light DOM. `.ns-button` 을 재사용한 페이지 이동 컨트롤 |
```

클래스 표에 두 줄을 추가한다.

```markdown
| `.ns-table` | `table`. `th`/`td` 는 자손 선택자 |
| `.ns-table-sort` | `th` 안의 `button` |
```

"왜 이런 구조인가" 절에 한 항목을 추가한다.

```markdown
**표는 데이터가 아니라 컨트롤을 소유한다.** 실사용 셀에 조건부 버튼 여섯과 참조 조회가 들어 있어 데이터 주입형 API 로 표현되지 않고, Lit 엘리먼트는 소비자의 React 렌더 함수를 호출할 수 없다. 그래서 `ns-table` 은 정렬 · 선택 **상태**만 갖고 셀 마크업은 소비자가 쓴다. 근거는 `docs/superpowers/specs/2026-08-13-ns-table-design.md` §1~2 에 있다.
```

디렉터리 트리에 두 줄을 추가한다.

```
│   ├── components/table/ns-table.ts        ReactiveElement. .styles.ts 가 없다(Light DOM)
│   ├── components/pagination/ns-pagination.ts  LitElement + light DOM 렌더
```

"남은 일" 에서 `ns-table` 항목을 지우고 이관 항목을 남긴다.

- [ ] **Step 4: `adding-a-component` 스킬에 Light DOM 체크리스트를 추가한다**

"## 먼저: 태그인가 클래스인가" 절 **다음**에 넣는다.

```markdown
## 태그라면: shadow 인가 Light DOM 인가

- **`controls.css` 의 클래스를 재사용해야 하면 Light DOM.** 전역 스타일시트는 shadow 안에 도달하지 않는다
- **스타일 캡슐화가 필요하면 shadow.** 기본값이다

Light DOM 이면 두 가지가 더 갈린다.

- **소비자 자식을 품는가** → `ReactiveElement` (`ns-table`). `LitElement` 는 템플릿으로 그 자식을 덮어쓴다
- **자식이 없고 렌더만 하는가** → `LitElement` (`ns-pagination`)

**둘 다 `createRenderRoot() { return this }` 를 재정의한다.** 빠뜨리면 shadow root 가 생겨 자식이 가려지거나, 스타일이 안 먹는다. `docs/gotchas.md` 의 "Light DOM 에서 소비자 자식이 사라지는 두 경로" 를 읽는다.

**Light DOM 컴포넌트에는 `.styles.ts` 를 만들지 않는다.** `static styles` 가 무시된다.
```

"## 태그를 추가할 때" 체크리스트에 한 줄을 더한다.

```markdown
- [ ] `src/controls/controls.css` — Light DOM 컴포넌트라면 요소 선택자로 스타일. `check-controls.mjs` 가 정방향으로 대조한다
```

- [ ] **Step 5: `releasing` 스킬의 기대 export 목록을 갱신한다**

콜드 설치 스모크 테스트가 기대하는 목록에 `NsPagination` 과 `NsTable` 을 알파벳 위치에 넣는다. `src/index.ts` 를 읽어 실제 출력과 맞춘다.

- [ ] **Step 6: 검증한다**

Run: `npm run check && npm run build`

```sh
grep -c '<script>' index.html                                        # 1
grep -n '</script>' index.html | grep -v -E ':\s*</script>\s*$' | grep -v '<script src='   # 출력 없음
grep -n 'document.addEventListener' index.html                        # 출력 없음
grep -rn 'ns-table' docs/project-structure.md | head -3               # 표에 들어갔는지
```

문서 주장이 코드와 맞는지 spot-check 셋을 한다 — `ns-table` 이 정말 `ReactiveElement` 를 상속하는지, `createRenderRoot` 재정의가 두 파일에 다 있는지, 두 컴포넌트 디렉터리에 `.styles.ts` 가 **없는지**. 어긋나면 문서가 틀린 것이므로 전사하지 말고 보고한다.

- [ ] **Step 7: 사람이 확인할 것을 보고서에 적는다**

- `index.html` 전체를 열어 새 섹션 셋이 보이고 콘솔 오류가 없는지
- 좌측 네비게이션의 새 항목 넷이 해당 섹션으로 이동하는지
- README 와 `project-structure.md` 의 표가 실제 export 와 맞는지

- [ ] **Step 8: 커밋**

```bash
git add -A
git commit -m "docs(table): Light DOM 규칙과 함정 기록"
```
